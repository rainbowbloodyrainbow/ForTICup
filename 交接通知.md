# 给新对话的临时交接说明

## 为什么切换对话

此前对话的可写工作区仍是：

```text
/mnt/32FA76A4FA7663CF/study/JustStudyEmbedded/MSPM0
```

用户已经把项目复制并整理到新的正式工作区：

```text
/mnt/32FA76A4FA7663CF/study/ForTICup
```

旧对话能够读取 `ForTICup`，但没有该目录的常规写权限，因此转到以
`ForTICup` 为工作区的新对话继续。后续只修改 `ForTICup`，不要再修改旧的
`JustStudyEmbedded/MSPM0`。

本文件是一次性交接文件。新对话读完并确认接管后，用户可决定是否删除。

## 开始工作前

1. 完整阅读根目录的 `Codex日志.md`、`myownlib使用说明.md` 和本文件。
2. 检查当前 Git 状态、实际目录结构及是否存在 `AGENTS.md`，保留用户已有改动。
3. 注意 `ForTICup/Codex日志.md` 可能尚未包含旧对话最后新增的
   `2026-07-28 21:00` 记录；本文件后面的最新规划可作为交接依据。
4. 不要擅自更新日志；仍然只有用户明确要求时才写日志。

## 项目方向

电赛正式工程不再采用“每个 Example 各自一套工程”的方式，而是只保留一套正式的：

```text
根目录 main.c
根目录 Makefile
根目录 SysConfig
一套 SysConfig 生成文件
```

`Examples/` 继续保留作学习和硬件验证历史参考，但根 Makefile 不得编译它们。

目标分层为：

```text
00_generated/    SysConfig 生成文件
01_platform/     数字 GPIO 输出、定时器 PWM、八路 ADC 读取、单调毫秒时间
02_device/       motor、encoder、servo、hc05、mpu6050、line_sensor
03_algorithm/    PID、一阶低通、移动平均、角度归一化、最短角差、数值限幅
04_control/      wheel_control、heading_control、line_control
05_robot/        Ackermann chassis、odometry
06_motion/       非阻塞定距直行、相对航向转弯、定半径圆弧、受控停车、立即中止
07_application/  具体赛题状态机，当前暂不实现
```

当前目录为：

```text
myownlib/01_platform/
├── adc/
├── hc05/
├── mpu6050/
└── output/
```

第一轮重构时，`output`、`adc` 留在 `01_platform`；`hc05`、`mpu6050`
应移动到新建的 `02_device`。不要因为分层而复制公共实现。

## 首版模块封闭范围

以下清单定义第一版允许实现的模块和职责。没有列入清单的功能不应被新对话自行
加入；需要扩展时先向用户说明依赖、资源和风险。

### `00_generated`

- 只保存唯一正式 SysConfig 生成的 C、H、链接脚本、库选项和事件图文件。
- 不手动修改生成文件。
- `Examples/` 内原有生成文件不属于正式工程。

### `01_platform`

- `output`：数字输出高、低、按布尔值写入、翻转；定时器启动、停止；指定通道
  的 `0～1000` 千分比 PWM。
- `adc`：读取指定 ADCMEM；一次获得 ADC0_A0～ADC0_A7 八路原始值；原始值按
  给定参考电压换算毫伏。传感器标定和循线位置计算不进入这一层。
- `system_time`：提供单调递增毫秒时间、控制周期序号和无符号回绕安全的时间差。
  它不动态分配定时器，不在 ISR 中运行控制算法，不实现任务调度器。

### `02_device`

- `motor`：TB6612 单路电机实例、有符号千分比输出、安装方向反相、最大输出限幅、
  正转、反转、滑行、短路制动、STBY 总使能、换向归零保护。
- `encoder`：单路编码器实例、自由运行累计计数、相邻采样增量、方向极性、
  计数回绕、PPS。第一版不伪造未知 CPR，不把轮径放进编码器。
- `servo`：舵机 PWM 脉宽、中位、安全上下限、安装反相、归中、按标定关系请求
  前轮转角。参数无效时只允许归中。
- `hc05`：现有字节、缓冲区、字符串和整数发送；UART RX 中断入队；主循环读队列；
  溢出查询和清除。第一版仍为单接收实例和阻塞发送。
- `mpu6050`：现有单设备非阻塞初始化、I2C 中断状态机、十四字节原始数据、
  状态查询、陀螺仪换算。采样调度、零偏校准、角度积分不进入设备层。
- `line_sensor`：八路原始 ADC、每路黑白标定、归一化强度、可配置位置权重、
  加权质心、强度和、有效标志。短时丢线保持和搜线策略不进入设备层。

### `03_algorithm`

- `pid`：P、PI、PID；实例状态；积分限幅；输出限幅；条件抗积分饱和；复位；
  显式 `dt`。
- `low_pass_filter`：一阶低通实例、系数配置、更新和复位。
- `moving_average`：固定容量窗口、更新、当前平均值和复位。
- `angle_math`：角度归一化到选定区间、两个角度的最短有符号角差。
- `numeric`：浮点和整数限幅、绝对值安全辅助。不得依赖 MSPM0 DriverLib。

### `04_control`

- `wheel_control`：目标 PPS、编码器实测 PPS、线性前馈、启动死区补偿、PI 更新、
  输出到 `motor`、控制器复位、失效时归零。左右轮使用两个独立实例。
- `heading_control`：接收目标航向和外部提供的实测航向，计算转向修正；不直接读取
  MPU6050，不直接写舵机。
- `line_control`：接收 `line_sensor` 结果，执行位置低通、误差死区、短时丢线保持、
  长时丢线停车或沿最后方向搜线、弯道减速、PD、D 项低通、非线性转向项和输出
  限幅；输出期望车速与期望曲率或转向修正，不直接写电机。

### `05_robot`

- `chassis`：阿克曼底盘参数、目标车速、目标曲率、前轮转角限制、左右后轮目标
  速度分配、舵机目标转角、统一停机。它不包含赛题流程。
- `odometry`：根据左右后轮编码器增量和前轮转向角更新二维 `x`、`y`、航向和累计
  距离；支持设置初始位姿和复位。所有几何量来自可标定配置。

### `06_motion`

首版只提供下面五种命令：

1. 定距直行：有符号距离、目标速度、加速度限制、减速度限制。
2. 相对航向转弯：有符号目标航向变化、目标速度、最大前轮转角。
3. 定半径圆弧：有符号半径、有符号圆心角、目标速度。
4. 受控停车：按照配置的减速度把目标速度降到零，完成后保持停止。
5. 立即中止：立刻把左右轮目标速度设为零，执行配置的安全停止方式，并把舵机
   请求设为中位。

每条运动命令均为非阻塞状态机，主循环周期调用 `Motion_Process()`。状态集合封闭为：

```text
IDLE
RUNNING
COMPLETED
CANCELLED
TIMEOUT
ERROR
```

每条命令必须具有启动参数校验、运行超时、完成容差、取消入口、当前状态查询和
错误原因查询。同一时间只允许一条运动命令运行。新命令不能静默覆盖正在运行的
命令。

相对航向转弯不是原地旋转；阿克曼底盘必须保持非零转弯半径。定距直行允许正向
和反向。定半径圆弧的正负号约定必须在头文件中明确。

### 首版明确不实现

- `07_application` 赛题状态机。
- NRF 无线协议驱动。
- 串口屏协议和界面。
- 超声波测距驱动。
- SSD1306 显存、字体和绘图。
- FreeRTOS、任务、队列和信号量。
- HC-05 TX 中断队列和 TX DMA。
- 多 MPU6050 实例和多设备 I2C 总线仲裁。
- Flash 参数持久化。
- 自动路径规划、障碍物绕行、地图和目标识别。

NRF、串口屏和超声波只保留引脚，不在第一版正式构建中初始化对应外设；赛题需要
其中某项时，由用户明确扩大范围后再加入。

## 正式目标板

正式目标板为 80 针“天猛星”，MCU 是 MSPM0G3507，统一 SysConfig 使用：

```text
MSPM0G3507
LQFP-64(PM)
```

此前 40 针“地猛星”示例使用的 LQFP-48 配置不再作为正式工程封装。

## 最终引脚与资源规划

### TB6612 与两个后轮电机

采用标准 TB6612 接法，不把 PWMA/PWMB 固定接高：

```text
左电机 PWMA       PA12  TIMG0_C0
右电机 PWMB       PA13  TIMG0_C1

左电机 AIN1       PB10  GPIO
左电机 AIN2       PB11  GPIO
右电机 BIN1       PB12  GPIO
右电机 BIN2       PB13  GPIO

TB6612 STBY        PA7   GPIO
```

标准 TB6612FNG 模块没有 `FAULT` 引脚，原计划的 PA15 已释放。

`motor` 位于设备层，只负责有符号输出、正反转、限幅、极性、刹车/滑行和
换向保护；编码器测速、前馈和速度 PID 属于 `wheel_control`。

设备层输出建议继续采用与现有 PWM 一致的 `-1000～1000` 整数千分比。
电机死区补偿初期放在 `wheel_control`，不要在 `motor` 中强制把小输出抬高。

### 转向舵机

```text
舵机 PWM          PA14  TIMG12_C0
```

需要新增 `02_device/servo`。舵机中位、最小/最大安全脉宽、安装反相和
脉宽到转角的映射均为可调参数；未知时默认只回中，不允许直接打满。

### 两个编码器

```text
左编码器 A        PB2   TIMG6_C0，拟作硬件边沿计数
左编码器 B        PB3   GPIO，方向输入

右编码器 A        PB4   TIMA1_C0，拟作硬件边沿计数
右编码器 B        PB5   GPIO，方向输入
```

MSPM0G3507 只有一套适合 SysConfig QEI 的 TIMG8，无法建立两套完整硬件
QEI。第一版以硬件累计 A 相脉冲、B 相辅助方向为模板；编码器公共 API 不暴露
后端实现，等待实测最大脉冲频率后再决定最终解码方式。不要采用“读取后清零”
而可能漏脉冲的实现，优先使用自由运行累计值之差。

编码器减速比、PPR/CPR 和最大脉冲频率目前未知，先保留原始计数及 PPS，
不能伪造理论参数。

### 八路 ADC

```text
传感器1           PA27  ADC0_A0
传感器2           PA26  ADC0_A1
传感器3           PA25  ADC0_A2
传感器4           PA24  ADC0_A3
传感器5           PB25  ADC0_A4
传感器6           PB24  ADC0_A5
传感器7           PB20  ADC0_A6
传感器8           PA22  ADC0_A7
```

现有 `adc` 只支持 ADCMEM0 单通道，需要扩展为多通道/多 ADCMEM 读取；
`line_sensor` 再负责逐路黑白标定、归一化、质心位置和丢线有效性。

### MPU6050

```text
I2C0 SDA           PA0
I2C0 SCL           PA1
```

保留现有非阻塞 I2C 中断状态机。

### HC-05

```text
UART1 TX           PA8
UART1 RX           PA9
```

保留现有阻塞发送和 RX 中断循环队列；高频控制路径不得进行大段阻塞遥测。

### 串口屏预留

```text
UART2 TX           PB15
UART2 RX           PB16
```

### NRF 预留

```text
MOSI / SPI0 PICO   PB17
SCK                PB18
MISO / SPI0 POCI   PB19
CSN                PB23  GPIO
CE                 PB26  GPIO
IRQ                PB27  GPIO
```

### 超声波预留

```text
TRIG               PA28  GPIO 输出
ECHO               PA31  GPIO 输入
```

### 调试接口

```text
SWDIO              PA19
SWCLK              PA20
```

## 底盘与参数原则

底盘为两个联动转向前轮、两个独立驱动后轮的阿克曼结构，两只普通 310
电机驱动后轮。用户用普通尺初步测得：

```text
前轮外径          约 48 mm
后轮外径          约 68 mm
后轮宽度          约 27 mm
后轮轮距          约 168 mm
前后轴距          约 124 mm
```

这些数值不精确，绝对不能作为不可修改的精确常量写死。应集中放进配置结构，
并通过实际行驶距离和转角反推有效轮径、轮距、轴距及舵机转角映射。

同样需要实测和可配置的还有：

- 编码器 PPR/CPR、减速比和方向；
- 舵机中位、行程、安装反相及前轮实际转角；
- 电机最大安全输出、死区和换向保护时间；
- 左右轮前馈模型；
- PID、滤波和循线控制参数；
- 动作完成容差及超时。

初次模板必须安全：电机默认不启动、控制器默认不产生有效驱动、舵机只回中。

## 旧 ESP32 代码参考

旧循线车完整原型位于：

```text
/mnt/32FA76A4FA7663CF/study/JustStudyEmbedded/ESP32/
JustForStudyESPIDF/02_system/pid_test/main/temp.c
```

其中有价值的是：

- 五路模拟归一化和质心定位；
- 短时丢线保持及搜线；
- 弯道减速；
- 循线外环 PD 与非线性转向项；
- 左右轮前馈、死区和带抗饱和的 PI 速度内环；
- 外环输出轮速目标、内环输出 PWM 的串级结构。

不要直接移植 ESP32 的 LEDC、PCNT、ADC、FreeRTOS 和长浮点 `printf`。

## 接下来应完成的任务

按可验证的小步推进，不追求一次把整车参数调完。

### 第一阶段：统一正式工程

1. 整理 `myownlib` 分层，移动现有模块但不复制。
2. 建立根目录唯一的 `main.c`、Makefile 和 LQFP-64 SysConfig。
3. 生成唯一的 `00_generated`，让根 Makefile 只编译正式工程和 `myownlib`。
4. `Examples/` 保留但完全不参与正式构建。
5. 先完成 SysConfig CLI、普通构建及 `-Wall -Wextra -Werror` 构建。

### 第二阶段：平台和设备

1. 迁移并复核 `output`、`hc05`、`mpu6050`。
2. 将 `adc` 扩展到八通道。
3. 实现安全的 `motor`。
4. 实现可标定的 `servo`。
5. 实现后端可替换的 `encoder`。
6. 实现 `line_sensor`。

### 第三阶段：算法和控制

1. 实现纯算法 `pid`、一阶低通、移动平均、角度归一化、最短角差和数值限幅。
2. 实现单轮/双轮 `wheel_control`，支持前馈、死区和 PI。
3. 实现输入输出与硬件分离的 `heading_control`、`line_control`。
4. 参数集中管理，不散落硬编码。

### 第四阶段：整车、里程计与明确动作集合

1. 实现阿克曼 `chassis`，组合后轮轮速和前轮转向。
2. 实现可标定 `odometry`。
3. 实现本文件“`06_motion` 首版封闭范围”列出的全部动作、状态和故障行为。
4. 当前不实现 `07_application` 的具体赛题状态机。

所有涉及真实硬件行为的阶段都应由用户烧录实测后再继续调参；编译通过不能
冒充硬件验证成功。

## 当前代码状态与工具链

交接发生时，`ForTICup` 是从旧学习仓库复制出来的新 Git 仓库。正式的根目录
单工程尚未建立，现阶段仍能看到 `Examples/01`～`05` 的旧示例和固件产物。
不要误以为根工程已经配置完成。

现有公共实现及其状态：

- `output`：普通 GPIO 输出和 `0～1000` 千分比 PWM；双路 PWM 已实机验证。
- `adc`：当前仅阻塞读取 ADCMEM0，必须扩展后才能支持八路阵列。
- `hc05`：阻塞发送、UART RX 中断和 63 字节有效容量的循环队列；当前只支持
  一路接收实例，大量阻塞输出会扰乱控制周期。
- `mpu6050`：单设备、独占一路 I2C 的非阻塞中断状态机；初始化、100 Hz
  采样、Z 轴校准与积分已在旧示例实机验证。

旧示例和公共模块此前均通过 ARM GCC；每次正式变更必须执行：

```text
SysConfig CLI 生成成功
ARM GCC 编译和链接成功
-Wall -Wextra -Werror 严格构建成功
firmware.hex 生成成功
```

开发环境：

```text
MCU SDK       /home/clbritheking/ti/mspm0_sdk_2_11_00_07
SDK 版本      2.11.00.07
SysConfig     /home/clbritheking/ti/sysconfig_1.28.0/sysconfig_cli.sh
编译器        arm-none-eabi-gcc
当前既有工程 CPU 时钟为 32 MHz
```

SysConfig 负责时钟、引脚复用、外设实例和中断源；公共模块只操作已经配置好的
硬件。不要直接编辑自动生成的 `ti_msp_dl_config.c/.h`。

## 尚未确定但不阻塞模板的参数

以下内容必须显式保留为待标定配置，不要自行猜一个“精确值”：

- TB6612 电机 PWM 频率及最大安全占空比；
- 310 电机的具体减速比和编码器 CPR/PPR；
- 两路编码器的最终方向极性及最大 PPS；
- 舵机型号、中位、安全脉宽范围和脉宽—前轮转角关系；
- 八路传感器实际用途、黑白方向和逐路标定值；
- 控制周期、各层控制增益、滤波系数和运动完成容差；
- 理论几何参数与实际有效轮径、轮距、轴距之间的修正值。

模板应允许这些参数集中修改，并在参数无效时保持安全停机，不应因为缺少标定
而填入危险的有效输出。

## 与用户协作时的工作方式

- 用户当前目标是电赛快速搭建可复用模板，但仍希望理解代码；优先采用清楚、
  显式的 C 写法和有限抽象，不要为了“架构漂亮”一次制造大量空壳。
- 描述模块范围时禁止使用“等”“之类”“诸如此类”代替未列出的内容。首版范围
  必须使用封闭清单；如果某项尚未决定，应明确写成“未决定”，不能暗示还包含
  未命名功能。
- 用户明确要求不要追求毕其功于一役。每次先完成一个能编译、能单独验证的小层，
  再由用户烧录实测并反馈。
- 解释代码时优先使用状态变量、`while`、`if` 和明确分支；避免过早压缩逻辑。
- 中断只做必要的采样、计数或入队；控制计算、阻塞串口输出和复杂状态机放在
  主循环。
- 固定周期中断只表示“调度事件到了”，不保证主循环必定及时执行。用计数或
  序号检测漏周期，不要只用一个可能合并事件的 `bool`。
- 所有模块尽量采用实例结构体，避免旧模块“一套全局状态只能支持一个实例”的
  限制继续扩散；但不要为尚不存在的第二种硬件过度设计万能框架。
- 真实引脚优先使用 SysConfig 生成宏；应用和公共模块不得散落硬编码端口、实例
  和通道编号。
- 不删除 `Examples/`，不把它们加入根构建，也不覆盖用户未提交改动。
- 只有用户明确要求时才写 `Codex日志.md`。

## 新对话的建议起点

新对话应先只完成并汇报第一阶段，而不是一次生成 `01`～`06` 的全部代码：

1. 核对 `ForTICup` 当前 Git 状态和目录。
2. 给出准备执行的迁移清单，确认没有覆盖用户已有文件。
3. 整理 `01_platform`、建立 `02_device`，迁移现有模块。
4. 建立根 `main.c`、Makefile 和 LQFP-64 SysConfig。
5. 先让 `output`、`adc`、`hc05`、`mpu6050` 在唯一正式工程中生成并严格构建通过。

完成这个稳定基线后，再从 `motor` 开始逐模块实现和实测。
